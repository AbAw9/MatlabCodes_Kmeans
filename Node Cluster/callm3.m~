function [SM C n s] = callm3()
Data = xlsread('M3.xls');
[Adj n]= getAdj(Data);
SM = sparse(Adj);
s=sum(sum(Adj));

C=zeros(1,n);
c1=[];
c2=[];
c3=[];
c4=[];

P=sort([c1 c2 c3 c4])';
N =(1:n)';
find(P~=N)